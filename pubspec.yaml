name: chat_box
description: anonymous chat group

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 2.2.2

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  workmanager: ^0.4.1
  crypto: ^3.0.1
  http: ^0.13.4
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
